import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
        
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity) // Растягиваем VStack
        .background(Color.blue) // Устанавливаем фон
    }
}
